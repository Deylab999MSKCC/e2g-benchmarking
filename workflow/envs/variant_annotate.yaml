name: variant_annotate
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - pandas
  - numpy
  - pybedtools
  - openpyxl
  - ca-certificates
  - openssl
prefix: /data/deyk/karthik/mambaforge/envs/variant_annotate
